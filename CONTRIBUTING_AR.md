# دليل المساهمة 🤝

شكراً لاهتمامك بالمساهمة في FreeHD! هذا الدليل سيساعدك على البدء.

## السلوك المتوقع 💬

نتوقع من جميع المساهمين:
- احترام الآراء المختلفة
- التركيز على الموضوع
- الإشارة إلى الأخطاء بشكل بناء
- عدم التسامح مع التحرش أو التمييز

## كيفية البدء 🚀

### 1. fork المشروع

```bash
# اضغط على زر Fork في GitHub
```

### 2. استنساخ المشروع

```bash
git clone https://github.com/your-username/freehd.git
cd freehd
git remote add upstream https://github.com/ErsatzTV/ErsatzTV.git
```

### 3. إنشاء فرع للتميز

```bash
# تحديث main
git checkout main
git fetch upstream
git merge upstream/main

# إنشاء فرع جديد
git checkout -b feature/وصف-الميزة
# أو للإصلاحات
git checkout -b fix/وصف-الخلل
# أو للأعمال الجارية
git checkout -b wip/وصف-العمل
```

## معايير الكود 💻

### C# / .NET

```csharp
// اتبع معايير Microsoft C# Coding Conventions
public class MyService
{
    private readonly ILogger<MyService> logger;

    public MyService(ILogger<MyService> logger)
    {
        this.logger = logger;
    }

    public async Task ProcessAsync(CancellationToken cancellationToken)
    {
        // استخدم async/await
        await Task.Delay(100, cancellationToken);
    }
}
```

**النقاط المهمة:**
- استخدم `async/await` للعمليات غير المتزامنة
- أضف `ILogger` للتسجيل
- أضف `CancellationToken` عند الحاجة
- اكتب تعليقات واضحة بالعربية/الإنجليزية

### Docker

```dockerfile
FROM base:version AS stage-name

RUN apt-get update && \
    apt-get install -y package && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

COPY . /app/
WORKDIR /app
```

**النقاط المهمة:**
- استخدم multi-stage builds
- نقّف الصور (clean up)
- أضف التعليقات

## عملية الإسهام 📝

### 1. تطوير الميزة

```bash
# اختر فرعك
git checkout feature/وصف-الميزة

# عدّل الملفات
# اختبر التعديلات
```

### 2. الاختبار المحلي

```bash
# بناء المشروع
dotnet build

# تشغيل الاختبارات
dotnet test

# بناء الصورة
docker build -f docker/Dockerfile -t freehd:dev .
docker run -p 8409:8409 freehd:dev
```

### 3. Commit والـ Push

```bash
# عرض التغييرات
git status
git diff

# إضافة الملفات
git add .

# commit مع رسالة واضحة
git commit -m "نوع: وصف الرسالة

قائمة التغييرات:
- نقطة أولى
- نقطة ثانية

Fixes #123"

# push إلى فرعك
git push origin feature/وصف-الميزة
```

### 4. فتح Pull Request

[في GitHub]:
1. اذهب إلى المستودع
2. اضغط "Compare & pull request"
3. ملأ:
   - **العنوان**: وصف واضح ومختصر
   - **الوصف**: 
   ```markdown
   ## الوصف
   وصف مختصر للتغييرات

   ## النوع
   - [ ] Bugfix
   - [ ] Feature
   - [ ] Documentation
   - [ ] Performance

   ## الاختبار
   - [ ] اختبرت محلياً
   - [ ] أضفت اختبارات
   - [ ] لا توجد مشاكل جديدة

   Fixes #123
   ```

### 5. المراجعة والدمج

- سيتم مراجعة الـ PR
- قد نطلب تعديلات
- بعد الموافقة، سيتم دمجها

## رسائل Commit الجيدة ✍️

```
<نوع>: <موضوع>

<الجسم - اختياري>

<التذييل - اختياري>
```

### الأنواع:
- `feat`: ميزة جديدة
- `fix`: إصلاح خلل
- `docs`: توثيق فقط
- `style`: تنسيق (بلا تغيير logic)
- `refactor`: إعادة هيكلة بلا تغيير السلوك
- `perf`: تحسين الأداء
- `test`: إضافة/تعديل الاختبارات
- `chore`: تحديثات الـ dependencies وغيرها

### أمثلة:

```
feat: إضافة دعم الترجمات الخارجية

- إضافة خيار رفع ملفات SRT
- دعم ترجمات متعددة اللغات
- إضافة أيقونة الترجمة في الواجهة

Fixes #456
```

```
fix: إصلاح تأخير البث عند التبديل بين القنوات

المشكلة كانت في دالة reconnect() التي تترك
الاتصالات القديمة مفتوحة.

Fixes #789
```

## ما نبحث عنه 👀

✅ **سنقبل:**
- إصلاحات الأخطاء الموثقة
- تحسينات الأداء
- ميزات محدودة ومفيدة
- توثيق وأمثلة
- اختبارات شاملة

❌ **لن نقبل:**
- كود بلا أسلوب ثابت
- PR ضخمة بدون تقسيم
- مشاكل غير موثقة
- تغييرات كبيرة بدون نقاش

## الأسئلة الشائعة ❓

### كيف أختبر محلياً؟

```bash
# بناء صورة Docker
docker build -f docker/Dockerfile -t freehd:test .

# أو تشغيل مباشر
dotnet run --project ErsatzTV/ErsatzTV.csproj
```

### كيف أضيف ميزة كبيرة؟

1. افتح issue أولاً للنقاش
2. انتظر الموافقة
3. أنشئ PR مع مراجع للـ issue

### كيف أُبلغ عن خلل؟

```bash
افتح issue مع:
- وصف واضح للمشكلة
- خطوات التكرار
- النتيجة المتوقعة vs الفعلية
- البيئة (OS, Docker version, إلخ)
```

## الموارد 📚

- [Git Workflow داخلي](docs/git-workflow.md)
- [معايير الكود](docs/coding-standards.md)
- [البنية المعمارية](docs/architecture.md)
- [الاختبارات](docs/testing.md)

## الدعم 💬

- **أسئلة عامة**: GitHub Discussions
- **مشاكل تقنية**: اختبر `docs/` أولاً
- **نقاشات الميزات**: فتح Issue + Discussion

---

شكراً لك! مساهمتك مهمة لنا ❤️
